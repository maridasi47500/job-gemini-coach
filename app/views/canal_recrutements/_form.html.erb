<%= form_with(model: canal_recrutement) do |form| %>
  <% if canal_recrutement.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(canal_recrutement.errors.count, "error") %> prohibited this canal_recrutement from being saved:</h2>

      <ul>
        <% canal_recrutement.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :city_id, style: "display: block" %>
    <%= form.text_field :city_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
