<%= form_with(model: employeur_type) do |form| %>
  <% if employeur_type.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(employeur_type.errors.count, "error") %> prohibited this employeur_type from being saved:</h2>

      <ul>
        <% employeur_type.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
